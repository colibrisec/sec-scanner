description: >
  This command echos "Hello World" using file inclusion.
# What will this command do?
# Descriptions should be short, simple, and clear.
# Chainbench

steps:
  - run:
      environment:
        PARAM_REPO: $CIRCLE_REPOSITORY_URL
      name: Chain-Bench
      command: <<include(scripts/chainbench.sh)>>
