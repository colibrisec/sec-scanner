description: >
  Sign scans or SBOMs
parameters:
  type:
    type: string
    description: "SBOM or Vulnerability Report"
steps:
  - run:
      environment:
        PARAM_TYPE: <<parameters.type>>
      name: Cosign
      command: <<include(scripts/cosign.sh)>>
