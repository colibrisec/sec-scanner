description: >
  Run a DAST Web Scan using ZAProxy

executor: zap

parameters:
  url:
    type: string
    description: "URL to Scan"
steps:
  - run: java --version
  - web-scan:
      url: << parameters.url >>
